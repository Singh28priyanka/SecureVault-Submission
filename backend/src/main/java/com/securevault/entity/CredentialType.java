package com.securevault.entity;

/** Supported credential types from the Password Vault module. */
public enum CredentialType {
    WEBSITE_LOGIN,
    EMAIL_ACCOUNT,
    BANKING,
    SOCIAL_MEDIA,
    APPLICATION,
    API_KEY,
    SECURE_NOTE
}
